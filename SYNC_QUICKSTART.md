# Быстрый старт синхронизации

## Что сделано

Создана инфраструктура для синхронизации заметок и статусов между 3-4 пользователями через Supabase.

### Новые файлы:
- ✅ `sync.js` - класс для работы с Supabase (аутентификация, синхронизация, real-time)
- ✅ `auth.html` / `auth.js` - страница входа/регистрации
- ✅ `config.json.example` - шаблон конфигурации
- ✅ `SYNC_SETUP.md` - подробная инструкция по настройке

### Что хранится в БД:
```
notes:
  - issue_key (JIRA-123)
  - text (текст заметки)
  - created_by, updated_by (кто создал/изменил)
  - created_at, updated_at (когда)
  - team_id (команда)
  
issue_statuses:
  - issue_key (JIRA-123)
  - status_id (ID из настроек расширения)
  - set_by (кто установил)
  - set_at (когда)
  - team_id (команда)
```

**НЕ хранятся чувствительные данные из Jira** (адреса, описания, тексты задач и т.п.)

## Как начать использовать

### 1. Создайте проект в Supabase

1. Зайдите на https://supabase.com и создайте аккаунт
2. Создайте новый проект (бесплатный tier)
3. Выполните SQL из `SYNC_SETUP.md` (раздел "Шаг 2" и "Шаг 3")

### 2. Настройте расширение

```bash
# Скопируйте файл конфигурации
cp config.json.example config.json

# Откройте config.json и вставьте свои данные из Supabase Dashboard → Settings → API:
{
  "supabaseUrl": "https://ваш-проект.supabase.co",
  "supabaseKey": "ваш-anon-public-ключ"
}
```

⚠️ **Важно**: используйте только `anon public` ключ, НЕ `service_role`!

### 3. Перезагрузите расширение

В Chrome: `chrome://extensions/` → кнопка обновления на карточке расширения

### 4. Войдите в аккаунт

Откройте любую страницу Jira → появится кнопка "🔐 Войти" → введите email/пароль

## Использование

### Для первого пользователя:
1. Войдите в аккаунт через `auth.html`
2. Создайте команду (будет добавлено в UI позже, пока через консоль):
```javascript
// В DevTools на странице Jira
const sync = new SupabaseSync();
await sync.init('url', 'key');
await sync.createTeam('Моя команда');
```

### Для остальных пользователей:
1. Войдите в аккаунт
2. Присоединитесь к команде по ID (первый пользователь даст ID команды)

### Автоматическая синхронизация:
- ✅ Заметки сохраняются локально И в Supabase
- ✅ Изменения других пользователей приходят в real-time (WebSocket)
- ✅ Работает offline - изменения синхронизируются при восстановлении связи
- ✅ Очередь несинхронизированных изменений

## Безопасность

### ✅ Что защищено:
- Row Level Security (RLS) на уровне PostgreSQL
- JWT токены с автообновлением
- HTTPS для всех запросов
- Пользователи видят только данные своей команды
- Audit log всех изменений

### ⚠️ Что НЕ хранится:
- Данные из Jira (описания, комментарии, адреса)
- Личная информация
- История задач из Jira

### 🔐 Хранится только:
- ID задачи (JIRA-123)
- Текст вашей заметки
- ID статуса (из настроек расширения)
- Email пользователя (для аудита)
- Метаданные (кто/когда создал/изменил)

## Следующие шаги

### Нужно добавить в UI:
- [ ] Кнопка "Войти/Выйти" в popup.html
- [ ] Управление командами (создать/присоединиться/переключиться)
- [ ] Индикатор статуса синхронизации
- [ ] История изменений (audit log)
- [ ] Список участников команды

### Нужно интегрировать в content.js:
- [ ] При сохранении заметки - вызывать `sync.saveNote()`
- [ ] При сохранении статуса - вызывать `sync.saveStatus()`
- [ ] Слушать события `note-updated` и `status-updated` для обновления UI
- [ ] Показывать, кто и когда изменил заметку/статус

## Стоимость

### Бесплатный tier Supabase (достаточно для 3-4 пользователей):
- 500 МБ database ✅
- 1 ГБ file storage ✅
- 2 ГБ bandwidth/месяц ✅
- 50,000 Monthly Active Users ✅

### Расчет для вашего случая:
- 100 задач × 500 байт/заметка = 50 КБ
- 100 статусов × 100 байт = 10 КБ
- **Итого: ~60 КБ** (0.012% от лимита)
- Бесплатного tier хватит на десятилетия

## Troubleshooting

### "Ошибка: не найден файл config.json"
→ Скопируйте `config.json.example` в `config.json` и заполните

### "Failed to initialize Supabase"
→ Проверьте URL и ключ в `config.json`
→ Убедитесь, что используете `anon` ключ, не `service_role`

### "No team selected"
→ Создайте или присоединитесь к команде

### Изменения не синхронизируются
→ Откройте DevTools → Console, посмотрите ошибки
→ Проверьте, что выполнены SQL скрипты из SYNC_SETUP.md (RLS политики)

## Документация

Подробная документация в `SYNC_SETUP.md`:
- Настройка Supabase
- SQL схема базы данных
- Безопасность и RLS политики
- GDPR compliance
- Альтернативные решения

## Поддержка

При возникновении проблем проверьте:
1. Консоль браузера (DevTools → Console)
2. Supabase Dashboard → Logs
3. Правильность SQL скриптов (включен ли RLS?)
