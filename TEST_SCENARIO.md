# 🧪 Сценарий тестирования извлечения данных из Jira

## ✅ Проверка реализации

### 1. Код проверен:

**content.js:**
- ✅ Функция `extractAndSaveAllIssueData()` динамически находит ВСЕ `customfield_*`
- ✅ Извлекает `label` (название поля) из `<h3>` в заголовке
- ✅ Извлекает `value` используя 5 разных стратегий поиска:
  - read-view элементы
  - inline-edit контейнеры  
  - select поля
  - date поля
  - user поля
- ✅ Фильтрует placeholder'ы: 'Нет', 'Введите текст', 'Добавьте варианты', 'Добавьте дату'
- ✅ Сохраняет в формате: `{ label: "Название", value: "Значение" }`
- ✅ Вызывается автоматически в `loadNotes()` при открытии карточки

**settings.js:**
- ✅ Функция `displayIssueData()` обработана корректно
- ✅ Динамически отображает все найденные поля
- ✅ Сортирует по номеру customfield
- ✅ Автоматически подбирает иконки по названию
- ✅ Показывает счетчик полей
- ✅ Добавляет title с ID поля при наведении

**settings.html:**
- ✅ Вкладка "📊 Данные карточек" создана
- ✅ Селектор для выбора карточки
- ✅ Контейнер для отображения данных
- ✅ Кнопки экспорта и удаления
- ✅ CSS стили для field-card

## 🎯 Сценарий тестирования

### Шаг 1: Загрузка расширения
```
1. Откройте chrome://extensions/
2. Включите режим разработчика
3. Обновите расширение или перезагрузите страницу Jira
```

### Шаг 2: Открытие карточки Jira
```
1. Откройте любую карточку Jira
2. Откройте Console (F12)
3. Проверьте логи:
   - "📊 Extracting full issue data for [KEY]..."
   - "🔍 Found X elements with customfield in testid"
   - "✓ customfield_XXXXX (Название): Значение"
   - "✅ Full issue data saved for [KEY]: X custom fields"
```

### Шаг 3: Просмотр в настройках
```
1. Кликните на иконку расширения
2. Нажмите "⚙️ Настройки"
3. Перейдите на вкладку "📊 Данные карточек"
4. Выберите карточку из списка
5. Проверьте отображение:
   - Заголовок: "Карточка: [KEY]"
   - Дата извлечения
   - Все кастомные поля с названиями и значениями
   - Счетчик полей внизу
```

### Шаг 4: Проверка данных
```
1. Нажмите "📥 Экспорт JSON"
2. Откройте скачанный файл
3. Проверьте структуру:
   {
     "issueKey": "SUPPORT-1234",
     "extractedAt": "2025-11-11T...",
     "fields": {
       "customfield_10094": {
         "label": "Соисполнитель",
         "value": "..."
       },
       "customfield_10206": {
         "label": "От кого задача",
         "value": "..."
       },
       ...
     }
   }
```

## 🔍 Что проверять

### В Console:
- [ ] Количество найденных элементов > 0
- [ ] Извлекаются названия полей (не пустые)
- [ ] Извлекаются значения (не placeholder'ы)
- [ ] Нет ошибок JavaScript

### В настройках:
- [ ] Карточка появилась в селекторе
- [ ] Отображаются все поля из консоли
- [ ] Иконки подобраны корректно
- [ ] Даты отображаются моноширинным шрифтом
- [ ] При наведении на карточку поля видно customfield_ID

### В localStorage:
```javascript
// В Console Jira страницы:
chrome.storage.local.get(null, (items) => {
  const issueData = Object.keys(items)
    .filter(k => k.startsWith('issuedata_'));
  console.log('Saved issues:', issueData);
  console.log('Example:', items[issueData[0]]);
});
```

## ⚠️ Возможные проблемы

### Не извлекаются поля:
- Проверьте структуру DOM в 1.html
- Возможно изменились testid селекторы Jira
- Добавьте дополнительные стратегии поиска в content.js

### Не отображаются названия:
- Проверьте что заголовки `<h3>` существуют
- Возможно нужен другой селектор для названия

### Пустые значения:
- Проверьте список placeholder'ов
- Возможно нужно добавить новые варианты

## 📊 Ожидаемый результат

Для карточки из примера (1.html) должно извлечься:
- customfield_10094 - Соисполнитель
- customfield_10206 - От кого задача  
- customfield_17754 - (название неизвестно)
- customfield_16741 - Channel of communication
- customfield_16769 - Tags
- customfield_20238 - Ping
- customfield_20237 - Ping Date
- customfield_11062 - Телеграм сотрудника / Employee's Telegram
- customfield_11118 - Проект/Департамент
- customfield_10229 - Отдел
- customfield_11174 - ГЕО
- customfield_11120 - Офис или Адрес
- customfield_11121 - Номер телефона для курьера
- customfield_11124 - (название неизвестно)
- customfield_11087 - Ваш телеграм/Your Telegram
- customfield_11138 - Другое ГЕО
- customfield_14246 - Задача с портала

**Итого: ~17+ кастомных полей должно быть извлечено и сохранено!**
