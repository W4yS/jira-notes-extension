# Jira Notes

Расширение для Chrome, которое добавляет заметки и статусы к задачам в Jira. Всё хранится локально в браузере.

## Что умеет

- Писать заметки к любой задаче
- Ставить цветные статусы (стандартные или свои)
- Показывать статусы цветными точками прямо на карточках доски
- **Автоматический сборщик копипасты** - собирает данные из полей задачи и создаёт готовый текст по шаблону
- Экспортировать/импортировать заметки

По умолчанию есть 4 статуса: � Проблема, 🟡 В процессе, 🟣 В фокусе, 🟢 Готово. Можно добавлять свои с любыми названиями, цветами и эмодзи.

## Установка

1. Скачайте код:
```bash
git clone https://github.com/W4yS/jira-notes-extension.git
cd jira-notes-extension
```

2. Откройте `chrome://extensions/`

3. Включите "Режим разработчика" вверху справа

4. Жмите "Загрузить распакованное расширение" и выберите папку с кодом

5. Создайте файл `code.json` (нужен только если хотите автоопределение офисов):
```bash
cp code.json.example code.json
```

Отредактируйте `code.json` под свои данные - туда пишите названия офисов и соответствующие им адреса.

Готово.

## Как пользоваться

**Заметки:**
- Откройте задачу в Jira
- Кликните на иконку расширения
- Пишите что хотите, сохранится само

**Статусы:**
- В той же панели выбирайте цветные кружки
- Статус отобразится точкой на карточке в доске

**Копипаста:**
1. Настройте шаблон: Иконка расширения → Настройки → вкладка "Шаблоны"
2. Перетащите нужные поля из списка справа в текстовую область
3. Добавьте текст между полями
4. Сохраните шаблон
5. Откройте любую задачу в Jira → кнопка "📋 Копипаста" в панели
6. Откроется окно предпросмотра - проверьте/отредактируйте текст
7. Нажмите "Копировать" (или Ctrl+Enter)
8. Готовый текст скопирован в буфер обмена

**Пример шаблона:**
```
Имя: {{customfield_10989}}
HR в Telegram: {{customfield_11121}}
Техника: {{customfield_11122}}
Периферия: {{customfield_11123}}
Адрес: {{customfield_11120}}
Телефон: {{customfield_11124}}
```

**Свои статусы:**
- Иконка расширения → Настройки → вкладка "Статусы"
- Добавляйте/удаляйте

**Офисы:**
- Если в задаче есть поле с адресом, расширение найдёт соответствие в вашем `code.json`
- Вместо "TASK-1234" на карточке покажет кодировку 
- Если не нашло - будет "ХЗ"
- Можно отключить в настройках (чекбокс "Автоопределение офисов")

## Настройки

Откройте настройки (кликните на иконку расширения → шестерёнка). Там четыре вкладки:

**Синхронизация:**
- Переключатель автоопределения офисов (включить/выключить коды на карточках)

**Статусы:**
- Список всех статусов
- Форма для добавления новых
- Кнопка сброса к стандартным

**Данные:**
- Просмотр всех извлечённых данных из карточек Jira
- Экспорт/удаление данных

**Шаблоны:**
- Конструктор шаблона копипасты
- Список доступных полей для перетаскивания
- Предпросмотр заполненного шаблона

## Структура файлов

```
jira-notes-extension/
├── manifest.json          # настройки расширения
├── content.js             # весь основной код
├── popup.html/js          # всплывающее окно
├── settings.html/js       # страница настроек
├── styles.css             # стили
├── code.json              # ваши офисы 
└── code.json.example      # пример структуры
```

## Где хранятся данные

В `chrome.storage.local`. Ничего никуда не отправляется, всё только у вас в браузере.

## Что делать если что-то сломалось

- `F5` на странице Jira
- Если не помогло - перезагрузите расширение в `chrome://extensions/`
- Если совсем всё плохо - `F12` и смотрите консоль

## Лицензия

MIT
